workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH'

stages:
  - deps
  - static-check
  - unit-test
  - integration-test
  - build

include:
  - local: .gitlab/backend.yml
  - local: .gitlab/frontend.yml
